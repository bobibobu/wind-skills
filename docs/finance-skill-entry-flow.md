# Wind AIMarket 金融 Skill 入口流程

## 目标

`https://aimarket.wind.com.cn/skill.md` 作为公开入口，只推荐用户安装 `wind-find-finance-skill`。完整能力索引和边界不应长期内置在入口 skill 包里，而应由 `aimarket.wind.com.cn` 提供远程能力中心。

用户安装入口 skill 后，客户自己的 Agent 根据远程 `manifest.json` 和必要的 wiki 文档判断该安装 / 调用哪个具体金融 skill。

## 流程图

```text
┌──────────────────────────────────────────────┐
│ 1. 公开入口                                  │
│ https://aimarket.wind.com.cn/skill.md        │
└───────────────────────┬──────────────────────┘
                        │
                        │ 只推荐安装入口 skill
                        v
┌──────────────────────────────────────────────┐
│ 2. 安装 wind-find-finance-skill              │
│ npx skills add Wind-Information-Co-Ltd/wind-skills ... │
└───────────────────────┬──────────────────────┘
                        │
                        v
┌──────────────────────────────────────────────┐
│ 3. 用户提出金融问题                          │
│ 如：茅台最新价 / 沪深300收益率 / 美股股价    │
└───────────────────────┬──────────────────────┘
                        │
                        v
┌──────────────────────────────────────────────┐
│ 4. 客户自己的 Agent 触发入口 skill           │
│ wind-find-finance-skill                      │
└───────────────────────┬──────────────────────┘
                        │
                        v
┌──────────────────────────────────────────────┐
│ 5. 入口 skill 拉远程能力地图                 │
│ https://aimarket.wind.com.cn/skills/manifest.json │
└───────────────────────┬──────────────────────┘
                        │
                        v
┌──────────────────────────────────────────────┐
│ 6. 根据 manifest 判断能力状态                │
└───────────┬────────────┬────────────┬─────────┘
            │            │            │
            v            v            v
┌────────────────┐ ┌────────────────┐ ┌────────────────┐
│ supported      │ │ partially      │ │ not_available  │
│ 已开放          │ │ supported      │ │ 暂未开放        │
└───────┬────────┘ │ 部分开放        │ └───────┬────────┘
        │          └───────┬────────┘         │
        │                  │                  │
        v                  v                  v
┌────────────────┐ ┌────────────────┐ ┌────────────────┐
│ 返回目标 skill  │ │ 返回目标 skill  │ │ 返回未开放说明  │
│ 安装命令        │ │ 覆盖边界        │ │ 边界文档 URL    │
│ wiki URL        │ │ wiki URL        │ └───────┬────────┘
└───────┬────────┘ └───────┬────────┘         │
        │                  │                  v
        │                  │          ┌────────────────┐
        │                  │          │ Agent 告诉用户 │
        │                  │          │ 当前暂未开放    │
        │                  │          └────────────────┘
        │                  │
        v                  v
┌──────────────────────────────────────────────┐
│ 7. 客户 Agent 判断目标 skill 是否已安装       │
└───────────────┬───────────────────┬──────────┘
                │                   │
                v                   v
┌────────────────────────┐ ┌────────────────────────┐
│ 未安装                  │ │ 已安装                  │
│ 展示或执行安装命令      │ │ 直接调用目标 skill      │
└────────────┬───────────┘ └────────────┬───────────┘
             │                          │
             v                          v
┌──────────────────────────────────────────────┐
│ 8. 目标 skill 调用 Wind MCP / API             │
│ 返回数据或文档结果                            │
└───────────────────────┬──────────────────────┘
                        │
                        v
┌──────────────────────────────────────────────┐
│ 9. 客户 Agent 组织答案返回给用户              │
└──────────────────────────────────────────────┘
```

## 异常分支

```text
┌──────────────────────────────────────────────┐
│ 拉 manifest 失败                              │
└───────────────────────┬──────────────────────┘
                        │
        ┌───────────────┴────────────────┐
        v                                v
┌────────────────────┐        ┌────────────────────┐
│ 有本地缓存          │        │ 无本地缓存          │
│ 使用缓存 manifest   │        │ 使用极小兜底规则    │
└────────────────────┘        └────────────────────┘
```

## 核心分工

```text
skill.md
= 公开入口，只推荐安装 wind-find-finance-skill

wind-find-finance-skill
= 薄入口，负责触发、拉 manifest、缓存、兜底、返回路由结果

manifest.json
= 机器可读能力地图，定义 supported / partially_supported / not_available

wiki / index.md
= 给客户 Agent 进一步理解边界时读取的说明书
```

## 设计原则

- 完整 wiki 不放进 `wind-find-finance-skill` 包里。
- 权威能力和边界放在 `aimarket.wind.com.cn`。
- 客户自己的 Agent 负责最终判断、安装、调用。
- Wind AIMarket 负责提供清楚、稳定、机器可读的能力地图。

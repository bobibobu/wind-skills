# Wind 金融数据 MCP 7 服务工具描述清单

本文档为 **直连 7 个 Wind MCP 服务、逐个 `tools/list` 实拉** 的工具描述与入参 Schema 全集，共 **7 服务 / 35 工具**。描述与参数一字不改地取自各服务后端官方返回，供路由、参数填写与接口对接参考。

- 实拉日期：2026-08-27
- 连接：`https://mcp.wind.com.cn/vserver_<domain>/mcp/`（结尾带斜杠），请求头 `Authorization: Bearer <API_KEY>`、`Accept: application/json, text/event-stream`、`Content-Type: application/json`；JSON-RPC 2.0，`tools/list` 列举工具、`tools/call` 调用。
- 返回示例（含实测数值）见姊妹文档 [`wind-mcp-skill-server-tools-io.md`](./wind-mcp-skill-server-tools-io.md)。

## 服务总览

| # | server_type | 服务 | 地址 | 工具数 |
| --- | --- | --- | --- | --- |
| 1 | `stock_data` | 股票（选股 + 档案/财务/股本/事件/技术/风险 + 行情/K线/分钟） | `.../vserver_stock_data/mcp/` | 10 |
| 2 | `fund_data` | 基金/ETF/LOF（筛选 + 档案/财务/持仓/业绩/持有人/公司 + 行情/K线/分钟） | `.../vserver_fund_data/mcp/` | 10 |
| 3 | `index_data` | 指数/板块（档案/基本面/技术 + 行情/K线/分钟） | `.../vserver_index_data/mcp/` | 6 |
| 4 | `bond_data` | 债券（基本档案/发债主体/行情估值/主体财务） | `.../vserver_bond_data/mcp/` | 4 |
| 5 | `financial_docs` | 金融文档 RAG（公告/新闻） | `.../vserver_financial_docs/mcp/` | 2 |
| 6 | `economic_data` | EDB 宏观/行业经济指标 | `.../vserver_economic_data/mcp/` | 2 |
| 7 | `analytics_data` | 通用分析数据（NL → Wind 数据） | `.../vserver_analytics_data/mcp/` | 1 |
| | | | **合计** | **35** |

> 入参约定：自然语言类工具传 `question`（公告/新闻传 `query`）；`windcode` 为名称或 Wind 代码（价格指标支持逗号分隔多标的）；`indexes` 为逗号分隔的指标字段名；日期格式 `yyyy-MM-dd`。K 线 `period` 取值：`1`=1分 `3`=5分 `4`=10分 `5`=15分 `6`=30分 `7`=60分 `8`=120分 `9`=240分 `10`=日K `11`=周K `12`=月K `13`=年K `14`=季K `15`=半年K。`count`（整数，默认 0）：正数从头往后取 N 条、负数从结束日期往前取 N 条、0 取全部，不超出日期区间。

---

## 1. stock_data 股票

服务地址：`https://mcp.wind.com.cn/vserver_stock_data/mcp/`

### 1.1 `get_stock_price_indicators`

> 获取一个或多个股票当前时刻的行情指标快照值（时点数据、非时间序列）。指标经 indexes 指定，取值须逐字取自其可选值清单（如 最新成交价、今日开盘价、今日最高价、今日最低价、涨跌幅、成交量、换手率等）；不填 indexes 时默认返回：最新交易日、交易时间、最新成交价、前收盘价、今日开盘价、今日最高价、今日最低价、成交量。仅返回时点截面，不含当日走势与历史区间序列。用户询问股票的当前/最新价格或某个行情指标值时使用。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 一个或多个股票名称或股票代码，多个代码用英文逗号分隔，如 600519.SH,600000.SH。 |
| `indexes` | | string | 最新交易日,交易时间,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,成交量 | 行情指标字段，多个字段用逗号分隔。可选值见下。 |

`indexes` 可选值：

```
最新交易日,交易时间,中文简称,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,最新均价,涨跌,涨跌幅,成交量,成交额,交易状态,上市日期,近1分钟成交额,近3分钟成交额,近5分钟成交额,K线实体涨跌幅,K线实体涨跌额,换手率,量比,振幅,基于Wind算法的量比,现量,现额,买一价,买二价,买三价,买四价,买五价,卖一价,卖二价,卖三价,卖四价,卖五价,买一量,买二量,买三量,买四量,买五量,卖一量,卖二量,卖三量,卖四量,卖五量,外盘,内盘,成交笔数,委比,当日主力净流入额,当日主力净流入占比,近5日主力净流入额,近5日主力净流入占比,近5日主力净流入天数,近10日主力净流入额,近10日主力净流入占比,近10日主力净流入天数,近20日主力净流入额,近20日主力净流入占比,近20日主力净流入天数,近60日主力净流入额,近60日主力净流入占比,近60日主力净流入天数,主力挂单买入,主力挂单卖出,主力撤单买入,主力撤单卖出,该日机构资金净流入额,该日大户资金净流入额,该日中户资金净流入额,该日散户资金净流入额,连红天数,连续上涨天数,火箭发射,高台跳水,涨停封板,跌停封板,涨停开板,跌停开板,涨幅达到3%,跌幅达到3%,创20日新高,创20日新低,盘前最新价,盘前涨跌额,盘前涨跌幅,盘前成交额,盘前涨速,盘后最新价,盘后涨跌幅,集合竞价涨跌幅,指数平滑异同移动平均,DIF快线,随机指标K值,随机指标D值,随机指标J值,6周期相对强弱指标,12周期相对强弱指标,抛物线转向指标,布林中轨,布林上轨,布林下轨,5周期移动平均,10周期移动平均,20周期移动平均,60周期移动平均,120周期移动平均,250日均线,5日乖离率,36日乖离,14周期顺势指标,26周期能量指标,12周期心理线指标,近1分钟涨跌幅,近3分钟涨跌幅,MACD多头金叉信号,MACD空头死叉信号,5分钟涨跌幅,5日涨跌幅,10日涨跌幅,20日涨跌幅,60日涨跌幅,120日涨跌幅,250日涨跌幅,年初至今涨跌幅,上市以来涨跌幅,近3年涨跌幅,近5年涨跌幅,近10年涨跌幅,近20年涨跌幅,流通份额,发行价,总股本,市净率,市净率(LF),市盈率(TTM),市盈率(LYR),市盈率(预测),总市值1,流通市值,总市值2,52周最高,52周最低,股息率,涨停价,跌停价,成分股贡献点数,近5分钟贡献度
```

### 1.2 `get_risk_metrics`

> 获取股票风险与波动性指标，涵盖Beta（标准、剔除财务杠杆、剔除所得税率）、Jensen Alpha（定期及年化）、波动率（年化及非年化，覆盖最近100周/24个月/60个月）、夏普比率（定期及年化）、最大回撤及区间下跌天数、在险价值VaR（参数法与历史法）、财务安全比率（资产负债率、速动比率、流动比率、净资产负债率）、以及基准相关系数。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：股票实体（名称或股票代码） + 量化风险指标（Beta、年化波动率、最大回撤、夏普比率、VaR等）+ 回看窗口或日期。示例：查询宁德时代(300750.SZ)过去1年的Beta、年化波动率和最大回撤。 |

### 1.3 `get_stock_events`

> 返回指定股票的公司行动与事件数据，为结构化记录而非公告文本。事件类型覆盖：首发上市与再融资（配股、增发、并购重组）；分红派息；股本与股东事件（大股东增减持、限售解禁）；治理与监管事件（风险警示与 ST 变动、违规处罚、司法诉讼）。仅返回结构化字段，不含公告与新闻文本原文，不含公司当前状态标签。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：股票实体（名称或股票代码）+ 事件类型（分红派息、增发配股、并购重组、增减持、限售解禁、ST变动、诉讼处罚等）+ 日期或时间范围。示例：查询贵州茅台(600519.SH)的分红派息历史。 |

### 1.4 `get_stock_kline`

> 返回指定股票在给定时间范围内的聚合量价序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。每条记录代表一个周期，包含开盘价、收盘价、最高价、最低价、成交量、成交额、均价、换手率。当日盘中分钟走势建议用分钟级行情工具（缺省即最新交易日）；锚定当前的相对窗口统计（近 N 日涨跌幅、年初至今涨跌幅、52 周高低）与基准指数对比不在本工具范围。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 股票名称或者股票代码，如贵州茅台或600519.SH。 |
| `begin_date` | ✅ | string | — | 开始日期：格式yyyy-MM-dd，如2026-03-25。 |
| `end_date` | ✅ | string | — | 结束日期，格式为yyyy-MM-dd,如2026-03-26。 |
| `period` | | string | 10 | K线周期，取值见文首约定。 |
| `count` | | integer | 0 | 区间内取数条数；正数从头取、负数从结束日往前取、0 取全部。 |
| `aftype` | | string | 0 | 复权类型：0=前复权，1=后复权，2=不复权。 |
| `issusp` | | string | 1 | 是否包含停牌数据：0=不包含，1=包含。 |
| `afdate` | | string | — | 复权基准日期，格式yyyy-MM-dd。通常不需要指定。 |

### 1.5 `get_stock_basicinfo`

> 返回指定股票的公司身份与分类档案，为静态数据，包括：简称、代码、曾用名、上市板块与上市日期、行业分类、概念标签、指数成份归属、工商注册信息、主营业务简介，以及当前状态标签（是否 ST、是否已摘牌）。仅返回当前状态，不含状态变动历史、财务数据与股东明细。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：股票实体（名称或股票代码）+ 需要的档案项（简称、上市板、上市日期、WIND行业分类、所属概念板块、经营范围、主营产品、是否风险警示等）。静态或准静态档案，通常无需时间限定。示例：查询贵州茅台的上市板与行业分类。 |

### 1.6 `get_stock_equity_holders`

> 返回指定股票的股本结构与股东构成，为准静态数据，随报告期与权益变动更新，包括：总股本、流通 A 股、限售股、自由流通股本及占比；前十大股东与前十大流通股东持仓及变动；机构股东持股；实际控制人与大股东详情；限售解禁时间表与本期解禁数量。不含市值（由价格与股本派生）。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：证券实体 + 股本/股东指标（总股本、流通A股、限售股、前十大股东、解禁时间表等）+ 报告期。示例：查询贵州茅台(600519.SH)的前十大股东及流通A股占比。 |

### 1.7 `get_stock_fundamentals`

> 返回指定股票的两类基本面数据。第一类为财务原始指标：三大报表科目，偿债、盈利、成长、杠杆等财务比率（如 ROE、毛利率、资产负债率、速动比率），以及行业专项指标（如银行净息差、不良贷款率），时间维度为报告期。第二类为衍生估值指标：PE、PB、PS（TTM 口径）、股息率、总市值与流通市值及其历史分位数，随交易日变动。不含价格序列。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：证券实体（名称或Wind代码）+ 财务/估值指标 + 日期或报告期。示例：查询贵州茅台(600519.SH)2024-12-31的ROE、营业收入和净利润。 |

### 1.8 `get_stock_quote`

> 返回指定股票的分钟级量价序列，每条记录代表一分钟，包含开盘价、最新成交价、最高价、最低价、成交量、成交额、均价与换手率。时间范围由 begin/end 指定（含首尾），未指定默认最新交易日；单日约 240 条，跨日体积按天数放大，长区间建议改用 K 线工具的聚合周期。仅交易日有数据，非交易日返回空结果。当前时刻截面状态请用行情快照工具。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 股票名称或者股票代码，如贵州茅台或600519.SH。 |
| `begin` | | string | 最新交易日 | 开始日期，格式 yyyy-MM-dd；不可只传 end 不传 begin。 |
| `end` | | string | 最新交易日 | 结束日期，格式 yyyy-MM-dd；只传 begin 时返回 begin 至最新交易日。 |
| `count` | | integer | 0 | 区间内取数条数；正数从头取、负数从结束日往前取、0 取全部。 |

### 1.9 `get_stock_technicals`

> 返回指定股票由行情数据计算的派生指标，日频为主。相对窗口统计：近 N 日、周、月、年及年初至今涨跌幅，52 周最高最低价，相对基准指数表现，均锚定当前滚动。技术指标：趋势类、成交量类、超买超卖类、波动类（如 MACD、KDJ、RSI、BOLL 等）。技术形态：连续涨跌、创新高或新低检测、突破反转、涨跌停与连板状态等。不含原始行情序列、实时快照与风险统计量。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：股票实体（名称或股票代码）+ 技术指标或形态（MACD、KDJ、RSI、BOLL、均线、区间涨跌幅、连板/新高等）+ 日期。示例：查询贵州茅台(600519.SH)最近20日的收盘价与MACD。 |

### 1.10 `search_stocks`

> 根据自然语言筛选条件从 A 股及海外股票市场反查股票，返回符合条件的股票代码列表。支持按指标数值（行情、财务、估值、资金、技术等）与业务分类（行业板块、主题概念、主营业务）组合条件，返回的代码可传入各属性查询工具获取具体数据。已指定具体股票时不要调用本工具。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言选股问句，描述筛选条件（如市值、涨跌幅、行业、上市板等），返回符合条件的代码列表。例：筛选沪深市场市值超500亿且连续5日上涨的股票 |

---

## 2. fund_data 基金 / ETF / LOF

服务地址：`https://mcp.wind.com.cn/vserver_fund_data/mcp/`

### 2.1 `get_fund_price_indicators`

> 获取一个或多个场内交易基金（ETF/LOF）当前时刻的行情指标快照值（时点数据、非时间序列）。指标经 indexes 指定，取值须逐字取自其可选值清单（如 最新成交价、今日开盘价、涨跌幅、成交额、换手率等）；不填 indexes 时默认返回：最新交易日、交易时间、最新成交价、前收盘价、今日开盘价、今日最高价、今日最低价、成交量。仅返回时点截面，不含过程序列；场外基金的净值查询不在本工具范围。用户询问场内基金的当前/最新价格或某个行情指标值时使用。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 一个或多个基金名称或基金代码（场内 ETF/LOF），多个代码用英文逗号分隔，如510300.SH,588200.SH。 |
| `indexes` | | string | 最新交易日,交易时间,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,成交量 | 行情指标字段，多个字段用逗号分隔。可选值见下。 |

`indexes` 可选值：

```
最新交易日,交易时间,中文简称,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,最新均价,涨跌,涨跌幅,成交量,成交额,交易状态,上市日期,近1分钟成交额,近3分钟成交额,近5分钟成交额,近7日平均成交额,换手率,量比,振幅,基于Wind算法的量比,现量,现额,买一价,买二价,买三价,买四价,买五价,卖一价,卖二价,卖三价,卖四价,卖五价,买一量,买二量,买三量,买四量,买五量,卖一量,卖二量,卖三量,卖四量,卖五量,外盘,内盘,成交笔数,委比,当日主力净流入额,当日主力净流入占比,近5日主力净流入额,近5日主力净流入占比,近5日主力净流入天数,近10日主力净流入额,近10日主力净流入占比,近10日主力净流入天数,近20日主力净流入额,近20日主力净流入占比,近20日主力净流入天数,近60日主力净流入额,近60日主力净流入占比,近60日主力净流入天数,主力挂单买入,主力挂单卖出,主力撤单买入,主力撤单卖出,连续上涨天数,连红天数,火箭发射,高台跳水,涨停封板,跌停封板,涨停开板,跌停开板,涨幅达到3%,跌幅达到3%,创20日新高,创20日新低,盘后最新价,盘后涨跌幅,指数平滑异同移动平均,DIF快线,随机指标K值,随机指标D值,随机指标J值,6周期相对强弱指标,12周期相对强弱指标,抛物线转向指标,布林中轨,布林上轨,布林下轨,5周期移动平均,10周期移动平均,20周期移动平均,60周期移动平均,120周期移动平均,250日均线,5日乖离率,36日乖离,14周期顺势指标,26周期能量指标,12周期心理线指标,近1分钟涨跌幅,近3分钟涨跌幅,MACD多头金叉信号,MACD空头死叉信号,5分钟涨跌幅,5日涨跌幅,10日涨跌幅,20日涨跌幅,60日涨跌幅,120日涨跌幅,250日涨跌幅,年初至今涨跌幅,上市以来涨跌幅,近3年涨跌幅,近5年涨跌幅,近10年涨跌幅,流通份额,最新净值,上期净值,累计净值,最新净值增长率,年初以来净值增长率,成立以来净值增长率,近一周净值增长率,近一月净值增长率,近一季净值增长率,近半年净值增长率,近一年净值增长率,近两年净值增长率,近三年净值增长率,近五年净值增长率,贴水率,基金规模,七日年化收益率,万份基金收益,IOPV,流通市值,涨停价,跌停价
```

### 2.2 `get_fund_kline`

> 返回指定场内交易基金（ETF/LOF）在给定时间范围内的聚合价格序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。每条记录代表一个周期，包含开盘价、收盘价、最高价、最低价、成交量、成交额、换手率与均价。当日盘中分钟走势建议用分钟级行情工具（缺省即最新交易日）；场外基金无场内行情，其价格口径为净值（属业绩与评价数据）。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 基金名称或者基金代码，如588200.SH。 |
| `begin_date` | ✅ | string | — | 开始日期：格式yyyy-MM-dd，如2026-03-25。 |
| `end_date` | ✅ | string | — | 结束日期，格式为yyyy-MM-dd，如2026-03-25。 |
| `period` | | string | 10 | K线周期，取值见文首约定。 |
| `count` | | integer | 0 | 区间内取数条数；正数从头取、负数从结束日往前取、0 取全部。 |
| `aftype` | | string | 0 | 复权类型：0=前复权，1=后复权，2=不复权。 |
| `issusp` | | string | 1 | 是否包含停牌数据：0=不包含，1=包含。 |
| `afdate` | | string | — | 复权基准日期，格式yyyy-MM-dd。通常不需要指定。 |

### 2.3 `get_fund_financials`

> 返回指定基金的财务报表与分红数据，时间维度为报告期，包括：利润指标（如基金利润、份额利润）；收入与费用（如利息收入、投资收益、公允价值变动、管理费、托管费）；报告期口径的资产净值；分红记录（如分红次数、单位分红、分红总额、分红条款）。不含净值序列与业绩评价数据。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：基金实体 + 财务/分红指标（基金利润、份额利润、资产净值、利息/投资收益、管理费、托管费、分红等）+ 报告期。示例：查询易方达蓝筹精选(005827.OF)2024年的基金利润和管理费。 |

### 2.4 `get_fund_holdings`

> 返回指定基金的投资组合数据，随定期报告披露更新，包含两族。披露持仓族：资产类别构成（如股票、债券、存款占净值比例及期间变动）；重仓资产（如重仓股票、重仓债券、FOF 的重仓基金，及占流通股比例、持仓变动等指标）；行业配置（如申万、Wind、中信口径）。组合派生特征族：由持仓计算的组合层面指标（如持股集中度、组合估值、基金换手率等）。仅返回基金组合层面的数据，个券的详细属性需以对应代码查询相应实体的数据。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：基金实体（名称或基金代码）+ 持仓/配置指标（资产类别配置、重仓股、重仓债、FOF重仓基金、行业配置、持股集中度等）+ 报告期。示例：查询易方达蓝筹精选2024年年报的前十大重仓股。 |

### 2.5 `get_fund_company_info`

> 返回指定基金的管理人（基金管理公司）档案，为以基金为入口的引用实体查询，包括：公司基本信息（如名称、成立日期、注册资本、管理层）；基金经理团队（如人数、人均管理产品数、任职年限统计）；在管规模（如合计规模及排名、非货币规模、旗下基金数量）；公司层面资产配置。注意本工具返回的是管理人公司而非基金产品本身的数据，基金产品属性请使用其他基金属性查询工具。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：基金实体（名称或基金代码）+ 管理人公司档案项（在管合计规模与排名、非货币规模、基金经理团队、公司层面资产配置）。本工具返回管理人公司而非基金产品本身。示例：查询易方达蓝筹精选管理人的在管合计规模与基金经理人数。 |

### 2.6 `get_fund_quote`

> 返回指定场内交易基金（ETF/LOF）的分钟级价格序列，每条记录代表一分钟，包含价格、均价、成交量与换手率。时间范围由 begin/end 指定（含首尾），未指定默认最新交易日；单日约 240 条，跨日体积按天数放大，长区间建议改用 K 线工具的聚合周期。仅交易日有数据，非交易日返回空结果。场外基金无场内行情，其价格口径为净值（属业绩与评价数据）。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 基金名称或者基金代码，如588200.SH。 |
| `begin` | | string | 最新交易日 | 开始日期，格式 yyyy-MM-dd；不可只传 end 不传 begin。 |
| `end` | | string | 最新交易日 | 结束日期，格式 yyyy-MM-dd；只传 begin 时返回 begin 至最新交易日。 |
| `count` | | integer | 0 | 区间内取数条数；正数从头取、负数从结束日往前取、0 取全部。 |

### 2.7 `get_fund_holders`

> 返回指定基金的份额、规模与持有人结构，包括：最新规模（资产净值、份额总数）及期间变动；持有人结构（如个人与机构持有比例、持有人户数）；申购赎回情况（如报告期及单季度份额变动）。不含静态产品档案。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：基金实体 + 份额/持有人指标（资产净值、份额总数、持有人结构、机构/个人占比、申购赎回等）+ 报告期。示例：查询易方达蓝筹精选(005827.OF)的最新规模和持有人结构。 |

### 2.8 `get_fund_performance`

> 返回指定基金的净值、业绩度量与评价数据，覆盖三族。净值族：单位净值、累计净值与复权净值的序列及净值增长，净值是基金资产的单位估值，为场外基金的唯一价格口径，场内基金亦适用。业绩度量族：收益率（如区间收益率、年化收益率、绝对收益口径）与同类排名（如按多周期回报、按规模）；货币基金以万份收益、7 日年化收益率为专项业绩口径。评价族：风险调整指标（如 Alpha、Beta、夏普比率、最大回撤、波动率、跟踪误差）；能力归因指标（如选股能力、选时能力）；风格分析（如风格箱、风格暴露）；基金评级（为第三方评价结果，非净值派生指标）；ETF/LOF 专项指标（如折溢价率、IOPV、净流入额）。不含基金财务报表数据。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：基金实体（名称或基金代码）+ 业绩/净值/评价指标（区间收益、年化收益、同类排名、Alpha/Beta/夏普、最大回撤、跟踪误差、ETF折溢价/IOPV等）+ 时间范围。示例：查询易方达蓝筹精选近一年的收益率和最大回撤。 |

### 2.9 `search_funds`

> 根据自然语言筛选条件从全市场基金反查产品，返回符合条件的基金代码列表。支持按指标数值（业绩、规模、费率、持仓特征等）与产品分类（投资类型、风格、指数跟踪、管理公司等）组合条件，返回的代码可传入各属性查询工具获取具体数据。已指定具体基金时不要调用本工具。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言基金筛选条件，由指标及其筛选参数（运算条件、阈值、时间范围等）组成，无需指定具体基金实体。每个筛选指标可各自带时间窗口。筛选股票型基金中近一年收益率超20%的产品。 |

### 2.10 `get_fund_info`

> 返回指定基金的产品档案，为静态或准静态数据，包括：代码、简称、全称；投资类型与风格；业绩比较基准与风险等级；费率结构（如管理费、托管费、申购赎回费）；基金经理（现任与历任，如任职期限、管理规模）；生命周期记录（如成立、转型、清盘、更名）；运作状态（如申购赎回状态）；管理人与托管人；发行信息（如成立日期、发行规模）；指数跟踪信息（如跟踪指数、上市日期、封闭运作期）。不含规模与变动、业绩数据与持仓明细。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：基金实体（名称或基金代码）+ 档案项（投资类型与风格、业绩比较基准、费率、现任基金经理、管理人/托管人、成立与发行信息、跟踪指数等）。静态或准静态档案，通常无需时间限定。示例：查询易方达蓝筹精选的投资类型、管理费率和现任基金经理。 |

---

## 3. index_data 指数 / 板块

服务地址：`https://mcp.wind.com.cn/vserver_index_data/mcp/`

### 3.1 `get_index_technicals`

> 返回指定指数基于历史行情序列计算的派生指标，包括相对窗口统计（如区间涨跌幅、均线、波动）与量价资金流向指标（如 MFI）。不含原始点位序列与实时快照。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 获取指数技术指标及衍生指标，涵盖多周期涨跌幅（N日、周、月、年、区间）、趋向指标、反趋向指标、能量指标、量价指标、压力支撑指标、成交量指标、超买超卖指标、摆动指标、强弱指标、以及波动指标 |

### 3.2 `get_index_quote`

> 返回指定指数的分钟级点位序列，每条记录代表一分钟。时间范围由 begin/end 指定（含首尾），未指定默认最新交易日；单日约 240 条，跨日体积按天数放大，长区间建议改用 K 线工具的聚合周期。仅交易日有数据，非交易日返回空结果。当前时刻截面状态请用行情快照工具。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 指数名称或者指数代码，如沪深300或000300.SH |
| `begin` | | string | 最新交易日 | 开始日期，格式 yyyy-MM-dd；不可只传 end 不传 begin。 |
| `end` | | string | 最新交易日 | 结束日期，格式 yyyy-MM-dd；只传 begin 时返回 begin 至最新交易日。 |
| `count` | | integer | 0 | 区间内取数条数；正数从头取、负数从结束日往前取、0 取全部。 |

### 3.3 `get_index_kline`

> 返回指定指数在给定时间范围内的聚合点位序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。每条记录代表一个周期，包含开盘价、收盘价、最高价、最低价等。当日盘中分钟走势建议用分钟级行情工具（缺省即最新交易日）；相对窗口统计与技术指标不在本工具范围。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 指数名称或者指数代码，如沪深300或000300.SH |
| `begin_date` | ✅ | string | — | 开始日期：格式yyyy-MM-dd，如2026-03-25。 |
| `end_date` | ✅ | string | — | 结束日期，格式为yyyy-MM-dd,如2026-03-26。 |
| `period` | | string | 10 | K线周期，取值见文首约定。 |
| `count` | | integer | 0 | 区间内取数条数；正数从头取、负数从结束日往前取、0 取全部。 |

### 3.4 `get_index_fundamentals`

> 返回指定指数的基本面与估值指标，按成份股加权聚合口径（如加权 PE、PB、股息率）。成份股个股的财务明细需以个股代码查询相应实体的数据；不含非预定义口径的临时聚合计算。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：指数实体（名称或指数代码）+ 加权基本面/估值指标 + 时间（可选：加权财务指标填报告期如2024-12-31，缺省最新披露期；估值指标及其分位数填交易日，缺省最新交易日）。示例：查询沪深300的PE、PB和成份股加权营收。 |

### 3.5 `get_index_price_indicators`

> 获取一个或多个指数当前时刻的行情指标快照值（时点数据、非时间序列）。指标经 indexes 指定，取值须逐字取自其可选值清单（如 最新成交价、今日开盘价、涨跌幅、成交额等）；不填 indexes 时默认返回：最新交易日、交易时间、最新成交价、前收盘价、今日开盘价、今日最高价、今日最低价、成交量。仅返回时点截面，不含过程序列。用户询问指数的当前/最新点位或某个行情指标值时使用。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `windcode` | ✅ | string | — | 一个或多个指数名称或指数代码，多个代码用英文逗号分隔，如000300.SH,000905.SH。 |
| `indexes` | | string | 最新交易日,交易时间,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,成交量 | 行情指标字段，多个字段用逗号分隔。可选值见下。 |

`indexes` 可选值：

```
最新交易日,交易时间,中文简称,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,最新均价,涨跌,涨跌幅,成交量,成交额,交易状态,近1分钟成交额,近3分钟成交额,近5分钟成交额,换手率,量比,振幅,基于Wind算法的量比,上市日期,现量,现额,当日主力净流入额,当日主力净流入占比,近5日主力净流入额,近5日主力净流入占比,近5日主力净流入天数,近10日主力净流入额,近10日主力净流入占比,近10日主力净流入天数,近20日主力净流入额,近20日主力净流入占比,近20日主力净流入天数,近60日主力净流入额,近60日主力净流入占比,近60日主力净流入天数,连续上涨天数,连红天数,指数平滑异同移动平均,DIF快线,随机指标K值,随机指标D值,随机指标J值,6周期相对强弱指标,12周期相对强弱指标,抛物线转向指标,布林中轨,布林上轨,布林下轨,5周期移动平均,10周期移动平均,20周期移动平均,60周期移动平均,120周期移动平均,250日均线,5日乖离率,36日乖离,14周期顺势指标,26周期能量指标,12周期心理线指标,近1分钟涨跌幅,近3分钟涨跌幅,MACD多头金叉信号,MACD空头死叉信号,5分钟涨跌幅,5日涨跌幅,10日涨跌幅,20日涨跌幅,60日涨跌幅,120日涨跌幅,250日涨跌幅,年初至今涨跌幅,上市以来涨跌幅,近3年涨跌幅,近5年涨跌幅,近10年涨跌幅,近20年涨跌幅,市净率,市净率(LF),市盈率(TTM),市盈率(预测),总市值1,流通市值,52周最高,52周最低,上涨家数,下跌家数,平盘家数
```

### 3.6 `get_index_basicinfo`

> 获取指数基本档案信息，涵盖指数名称、发布机构、基日、基点、计算方法、成份股、成分数量、以及指数分类。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：指数实体（名称或指数代码）+ 需要的档案项（全称、发布机构、基日与基点、指数风格、成份数量、全收益/净收益指数代码、被跟踪的基金列表与数量等）。静态或准静态档案，通常无需时间限定。示例：查询沪深300的发布机构、基日基点与成份数量。 |

---

## 4. bond_data 债券

服务地址：`https://mcp.wind.com.cn/vserver_bond_data/mcp/`（四个工具均传 `question`，无行情快照/K线/分钟工具）

### 4.1 `get_bond_basicinfo`

> 返回指定债券的静态档案数据，包括发行要素（如发行规模、票面利率、期限、起息与到期日期）、信用评级、债券类型等。不含行情与估值数据、发债主体信息。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：债券实体（债券简称或债券代码）+ 需要的档案项（债券类型、发行规模、发行价格、票面利率、期限、起息与到期日期、兑付安排、债项信用评级、跨市场代码等）。静态或准静态档案，通常无需时间限定。示例：查询国债2601的发行规模、票面利率与到期日期。 |

### 4.2 `get_bond_issuer_info`

> 返回指定债券发债主体的档案信息，包括主体基本信息、主体信用评级等。债券属性与主体财务数据请使用相应工具。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：债券实体（债券简称或债券代码）+ 主体档案项（主体名称、注册地址、行业分类、股权结构、企业背景、主体信用评级等）。示例：查询22万科MTN001发债主体的注册地址、行业分类与最新主体评级。 |

### 4.3 `get_bond_market_data`

> 返回指定债券在指定时间范围内的区间行情与估值数据，频率支持日、周、月、年及自定义区间，包括价格、收益率、估值指标等。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：债券实体（债券简称或债券代码）+ 行情/估值指标（收盘价、成交量、到期收益率、估价收益率、久期、凸性、利差等）+ 时间范围与频率（可选，日/周/月/年或自定义区间，缺省最新交易日、日频）。示例：查询国债2601最新一日的到期收益率、久期和凸性。 |

### 4.4 `get_bond_financial_data`

> 返回指定债券发债主体的财务数据，时间维度为报告期。主体档案信息与债券本身属性请使用相应工具。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言查询要求：债券实体（债券简称或债券代码，返回其发行人的财务数据）+ 财务指标（营收、利润、资产、负债及财务比率等）+ 报告期（可选，填如2024-12-31，缺省最新披露期）。示例：查询22万科MTN001发债主体2024-12-31的营业收入和净利润。 |

---

## 5. financial_docs 公告 / 新闻

服务地址：`https://mcp.wind.com.cn/vserver_financial_docs/mcp/`（参数为 `query` + `top_k`）

### 5.1 `get_company_announcements`

> 检索并返回上市公司公告文本，支持按公司、公告类型、时间范围等条件检索。公告对应的结构化字段（如分红金额、事件日期）需查询相应实体的数据；第三方媒体报道不在本工具范围。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `query` | ✅ | string | — | 自然语言检索要求：检索目标（某主体、某事项的官方披露原文）+ 可选线索——公告类型（年报、季报、分红、招募说明书等）、时间范围。示例：查询贵州茅台2026年以来的分红派息相关公告。 |
| `top_k` | | integer（1–20） | 5 | 返回的相关文档或片段的最大数量。 |

### 5.2 `get_financial_news`

> 检索并返回财经新闻文本，支持按主题、实体、时间范围等条件检索。不包含发行人官方公告与券商研报；新闻中提及的结构化数据（如价格、财务数值）以相应实体的数据返回为准。

| 参数 | 必填 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `query` | ✅ | string | — | 自然语言检索要求：检索目标（主题、事件，无需必含具体实体）+ 可选线索——实体、行业、时间范围。示例：近一周美联储利率政策新闻。 |
| `top_k` | | integer（1–20） | 5 | 返回的相关文档或片段的最大数量。 |

---

## 6. economic_data 宏观 EDB

服务地址：`https://mcp.wind.com.cn/vserver_economic_data/mcp/`

### 6.1 `search_economic_indicator`

> 根据自然语言需求，从经济数据库中检索并匹配相关经济指标，返回指标的元信息（指标名称、指标代码、频率、单位、来源等），不提取指标数据。适用于查找可用指标、筛选指标及提数前确认指标的场景。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 查询内容。用户的自然语言搜索问句，如 中国近三年 GDP 相关指标、上海 CPI 有哪些、有哪些出口相关指标。 |

### 6.2 `query_economic_indicator_data`

> 用于自然语言获取 Wind EDB 宏观经济指标信息和时间序列数据（不回答概念、定义等无需访问 EDB 数据的问题）。当用户明确要求换算，或语境隐含需要将多个指标放同一口径下比较（如跨国/跨地区对比、统一看板、计算差值、排名等）时，需填写 targetMagnitude、targetFrequency、targetCurrency 参数，工具会在输出时间序列数据时自动转换并对齐数据。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 查询内容。自然语言搜索问句，如 提取中国GDP数据、查找上海 CPI 数据、帮我找到中国最新一期出口同比数据。 |
| `beginDate` | | string | 数据提取开始时间，格式 yyyy-MM-dd。与 observation 互斥，不应同时填写。 |
| `endDate` | | string | 数据提取结束时间，格式 yyyy-MM-dd。与 observation 互斥，不应同时填写。 |
| `observation` | | integer | 观测期数。提取近 N 期数据时填整数；与 beginDate/endDate 互斥。返回会对齐最低频次指标时间。 |
| `targetMagnitude` | | string（枚举） | 目标数量级/展示单位。枚举：十万分之一,万分之一,千分之一,百分之一,十分之一,个,十,百,千,万,十万,百万,千万,亿,十亿,百亿,千亿,万亿,十万亿,百万亿,千万亿,亿亿,十亿亿,百亿亿。用户明确要求单位转换、或存在数量级对齐意图（跨指标拼表、看板、差值、排名、多经济体体量对比等）时填。 |
| `targetCurrency` | | string | 目标币种，ISO 4217 三字母代码（CNY/USD/EUR/JPY/KRW/INR 等）。用户明确要求折算、或存在币种对齐意图（跨国对比/排名/换算）时填；任务要求可比但未指定时按业务默认（通常 USD）。 |
| `targetFrequency` | | string（枚举） | 目标频率。枚举：日,工作日,周,月,季,半年,年,年度。用户明确要求按某频率展示/重采样、或存在频率对齐意图（月度汇总成季度、年度拆分等）时填。 |

---

## 7. analytics_data 通用分析数据

服务地址：`https://mcp.wind.com.cn/vserver_analytics_data/mcp/`

### 7.1 `get_financial_data`

> 根据自然语言描述执行结构化数据的计算与提取，支持自定义指标组合、跨实体聚合与灵活的数据加工。仅当所需结果不能由任何预定义数据工具直接返回时使用本工具：单一实体的属性数据、指数与板块的预定义加权指标、宏观指标均调用相应数据服务；按条件筛选并返回实体列表使用各服务的筛选工具（本工具返回计算结果而非实体列表）。

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `question` | ✅ | string | 自然语言计算/取数要求：实体范围 + 指标或计算逻辑 + 日期/时间范围或报告期。用于跨标的聚合、加权平均、排名、复合指标推导等预定义工具无法直接返回的结构化结果。示例：查询中国A股市场过去一年的平均成交量。 |

---

*本文档所有工具描述与参数 Schema 均为 2026-08-27 直连 `mcp.wind.com.cn` 各服务 `tools/list` 实拉原文（Key 未写入本文档，占位 `<API_KEY>`）。字段以实际返回为准，后端更新后请重新拉取。*

# AIMarket Skill 安装与配置流程

## 完整流程图

```text
┌──────────────────────┐
│  Agent 阅读 skill.md  │
└──────────┬───────────┘
           │
           ▼
┌────────────────────────────────────┐
│  Agent 说明需一次性安装 2 个 skills │
│                                    │
│  wind-find-finance-skill           │
│  用于发现和推荐金融能力              │
│                                    │
│  wind-mcp-skill                    │
│  用于 Wind 数据底座和真实取数         │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  用户选择安装范围                   │
│                                    │
│  1. 安装到当前 agent                │
│  2. 安装到指定 agent                │
│  3. 安装到本机所有支持的 agent       │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  按用户选择安装两个 skills           │
│                                    │
│  -g 只代表用户级安装                 │
│  目标 agent 需要显式指定             │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────────────┐
│  安装完成后主动说明：                │
│  真实取数需要配置 WIND_API_KEY       │
└──────────┬─────────────────────────┘
           │
           ▼
┌────────────────────────────┐
│  Agent 检查是否已有 Key     │
└──────────┬─────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌──────────────┐  ┌────────────────────────────┐
│  已有 Key     │  │  没有 Key                  │
└──────┬───────┘  └─────────────┬──────────────┘
       │                        │
       ▼                        ▼
┌──────────────┐  ┌────────────────────────────┐
│  轻量取数验证 │  │  引导用户获取 Key           │
└──────┬───────┘  │                            │
       │          │  1. 用户自己打开官网         │
       │          │  2. Agent 经授权打开官网     │
       │          └─────────────┬──────────────┘
       │                        │
       │                        ▼
       │          ┌────────────────────────────┐
       │          │  用户获取 WIND_API_KEY      │
       │          └─────────────┬──────────────┘
       │                        │
       │                        ▼
       │          ┌────────────────────────────┐
       │          │  配置 Key                   │
       │          │  推荐全局，所有 Wind skills 共用 │
       │          └─────────────┬──────────────┘
       │                        │
       └────────────┬───────────┘
                    ▼
          ┌────────────────────┐
          │  轻量取数验证       │
          └─────────┬──────────┘
                    ▼
          ┌────────────────────┐
          │  完成，可开始使用    │
          └────────────────────┘
```

## 精简流程图

```text
┌──────────────────────┐
│  Agent 阅读 skill.md  │
└──────────┬───────────┘
           ▼
┌──────────────────────────────┐
│  告知用户需安装 2 个 skills   │
│                              │
│  wind-find-finance-skill     │
│  wind-mcp-skill              │
└──────────┬───────────────────┘
           ▼
┌──────────────────────────────┐
│  用户选择安装范围             │
│                              │
│  1. 当前 agent                │
│  2. 指定 agent                │
│  3. 本机所有支持的 agent       │
└──────────┬───────────────────┘
           ▼
┌──────────────────────────────┐
│  按选择安装两个 skills        │
│  -g 仅表示用户级安装           │
│  目标 agent 需显式指定         │
└──────────┬───────────────────┘
           ▼
┌──────────────────────────────┐
│  检查 WIND_API_KEY            │
└──────────┬───────────────────┘
           ▼
┌──────────────────────────────┐
│  有 Key：轻量取数验证          │
│  无 Key：引导用户获取并配置     │
└──────────┬───────────────────┘
           ▼
┌──────────────────────┐
│  验证通过，开始使用    │
└──────────────────────┘
```

## 说明

Agent 读取 `skill.md` 后，应先向用户说明 AIMarket 需要一次性安装两个基础能力：

- `wind-find-finance-skill`：用于发现和推荐金融能力。
- `wind-mcp-skill`：用于连接 Wind 数据底座，执行真实金融取数。

安装前应让用户选择安装范围：当前 agent、指定 agent，或本机所有支持的 agent。`-g` 只表示用户级安装，不等于自动安装到所有 agent；目标 agent 应通过 `--agent` 或安装器支持的选择机制显式指定。

安装完成后，Agent 应主动检查是否存在 `WIND_API_KEY`。如果已配置，就执行轻量取数验证；如果未配置，则引导用户访问 AIMarket 开发者中心获取 Key，并配置到推荐的全局位置。

配置完成且验证通过后即可开始使用。一般不需要默认要求重启；只有当前 agent 没有识别到新 skill 时，再提示用户刷新或重启对应 agent。
